export { KeyboardModule } from './keyboard.module'
export { KeyboardService } from './keyboard.service'
import { ClientKeyboards } from './client-keyboards'

export const Keyboards = {
  client: ClientKeyboards,
}