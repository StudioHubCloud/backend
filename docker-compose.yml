services:
  # api: 
  #   build:
  #     context: .
  #     dockerfile: ./apps/api/Dockerfile
  #     target: development
  #   command: npm run start:dev api
  #   container_name: studio_hub_cloud-api
  #   ports:
  #     - ${API_PORT}:${API_PORT}
  #   volumes:
  #     - .:/usr/src/app
  #   env_file:
  #     - ./apps/api/.env
  database:
    image: postgres:latest
    container_name: studio_hub_cloud-db
    ports:
      - 5432:5432
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
  # redis:
  #   image: redis:latest
  #   container_name: studio_hub_cloud-db=redis
  #   ports:
  #     - ${REDIS_PORT}:6379
  #   volumes:
  #     - redis_data:/data
